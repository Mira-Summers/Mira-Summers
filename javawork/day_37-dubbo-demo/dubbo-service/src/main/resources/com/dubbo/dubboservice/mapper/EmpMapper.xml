<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dubbo.dubboservice.mapper.EmpMapper">
    <select id="empList" parameterType="hashmap" resultType="dubbo.pojo.Emp">
        select eid,empName,birthday from tb_emp
    </select>

</mapper>